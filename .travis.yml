language: ruby
cache: bundler
sudo: required

addons:
 - firefox: "latest"

services:
  - docker

env:
  - APP_HOST="http://localhost:8089"

before_install:
- bundle install
- docker build -t rackcpp_hello .

script:
- docker run -d -p 8089:8089 rackcpp_hello
- bundle exec rake